class Member < ApplicationRecord
	# search_response.data.items.each do |search_result|
	#     case search_result.id.kind
	#         when 'youtube#video'

	#             member = Member.new(memberid: "#{search_result.id.memberId}", 
	#                             name: "#{search_result.snippet.name}" 
	#             member.save                   
	#     end
	# end

end
